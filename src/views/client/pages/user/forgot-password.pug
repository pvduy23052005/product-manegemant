doctype html
html(lang="vi")
  head
    meta(charset="UTF-8")
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
    title Quên Mật Khẩu
    link(rel="stylesheet" href="/client/css/auth.css")

  body
    .login-container
      .login-card
        //- Logo & Title
        .logo-container
          .logo
            svg(fill="none" stroke="currentColor" viewBox="0 0 24 24")
              path(stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 7a2 2 0 012 2m4 0a6 6 0 01-7.743 5.743L11 17H9v2H7v2H4a1 1 0 01-1-1v-2.586a1 1 0 01.293-.707l5.964-5.964A6 6 0 1121 9z")
          h1.login-title Quên mật khẩu?
          p.login-subtitle Nhập email để nhận liên kết đặt lại mật khẩu


        if error.length > 0 
          .error-message#errorMessage
            | #{error}

        //- Forgot Password Form
        form#forgotForm(action="/user/password/forgot" method="POST")

          //- Email
          .form-group
            label.form-label(for="email") Email
            .input-wrapper
              svg.input-icon(fill="none" stroke="currentColor" viewBox="0 0 24 24")
                path(stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 12a4 4 0 10-8 0 4 4 0 008 0zm0 0v1.5a2.5 2.5 0 005 0V12a9 9 0 10-9 9m4.5-1.206a8.959 8.959 0 01-4.5 1.207")
              input.form-input(
                type="email"
                id="email"
                name="email"
                placeholder="admin@example.com"
                required
              )

          //- Submit Button
          button.btn-login(type="submit") Gửi email xác nhận

        //- Back to Login Link
        .signup-link
          | Nhớ mật khẩu? 
          a(href="/user/login") Đăng nhập ngay

    script.
      const form = document.getElementById('forgotForm');
      const errorMessage = document.getElementById('errorMessage');

      if(errorMessage){
        errorMessage.classList.add('show');
          setTimeout(() => {
            errorMessage.classList.remove('show');
          }, 3000);
      }
